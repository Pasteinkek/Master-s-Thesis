%%%% PACKAGES

\usepackage{MA_Titlepage}					%prescribed title page by uni bonn

\usepackage{microtype}						%Makes Document nicer, circumvents overfull h-boxes

\usepackage{mathtools}						%includes "amsmath",  standard for mathematical typesetting

\usepackage{amssymb}
\usepackage{amsthm}							
\usepackage{stmaryrd}						%useful packages for mathematics

\usepackage{physics}						%also useful package for mathematics, e.g. the dd at the end of an integral, norm etc.

\usepackage[babel=true]{csquotes}				%Automates quoting things

\usepackage{enumitem}							%configuration of list enviroments, e.g. alphabetic enumeration

\usepackage[backend=biber,style=alphabetic]{biblatex}			%used for citations. biber is the program which accesses the bib file and passes it to latex, biblatex provides commands for citing

\usepackage{pgfplots}							%for plotting functions

\usepackage{dsfont}								% package for indicator function of a set

\usepackage{comment}							%package for larger comments

%-----> the following packages have to be loaded last

\usepackage[colorlinks=true,linkcolor=blue,unicode]{hyperref}						%puts hyperlinks in the pdf file for more convenience

\usepackage[capitalise,noabbrev]{cleveref}	%automated referencing, capitalise will give capitalised letters, noahbrev prevents abbrevations



%%%% THEOREM ENVIROMENTS (should be after loading cleverref, hyperref)

\newcounter{sometheorems}[section]
\renewcommand{\thesometheorems}{\thesection.\arabic{sometheorems}}									%changes how the theorem-like stuff is numbered
	
\newtheorem{theorem}[sometheorems]{Theorem}
\newtheorem{lemma}[sometheorems]{Lemma}
\newtheorem{corollary}[sometheorems]{Corollary}
\newtheorem{proposition}[sometheorems]{Proposition}
\newtheorem{definition}[sometheorems]{Definition}
\newtheorem{example}[sometheorems]{Example}

\theoremstyle{remark}						%provided by asmath package, remark will not be cursive
\newtheorem*{remark}{Remark}


%%%% STYLE OPTIONS

\frenchspacing									%no additional spacing after sentence

\binoppenalty = \maxdimen
\relpenalty = \maxdimen							%no linebreak for inline math


%%% COMMANDS

%general symbols

\DeclareMathOperator{\energy}{E}
\DeclareMathOperator{\rb}{\partial_{ \ast }}
\DeclareMathOperator{\hd}{\mathcal{H}}
\DeclareMathOperator*{\esssup}{ess\,sup}		%the * gives rise to limit-like enviroment
\DeclareMathOperator{\flattorus}{\mathbb{T}}
\DeclareMathOperator{\divg}{div}
\DeclareMathOperator{\diff}{D}
\DeclareMathOperator{\lm}{\mathcal{L}}
\DeclareMathOperator{\hm}{\mathcal{H}}
\DeclareMathOperator{\tilt_excess}{\mathcal{E}}
\DeclareMathOperator{\geodesic_distance}{\mathrm{d}_{W}}

%function spaces

\DeclareMathOperator{\lp}{L}
\DeclareMathOperator{\wkp}{W}
\DeclareMathOperator{\cont}{C}
\DeclareMathOperator{\bv}{BV}


\DeclarePairedDelimiterX{\inner}[2]{\langle}{\rangle}{#1 \, , \, #2}

